<h1>Your Gear Closet</h1>

<%= link_to 'New Item', new_item_path %>

<table align="center">
	<tr>
		<th>Title</th>
		<th>Text</th>
	</tr>

	<% @items.each do |item| %>
		<tr>
			<td><%= item.title %></td>
			<td><%= item.text %></td>
		</tr>
	<% end %>
</table>